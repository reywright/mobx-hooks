// Note: as more utitlies get fleshed out, and categories
// become more distilled, utils.js becomes a folder, separated
// out into more fine grained categories

export const incrementCount = count => {
  return count + 1;
};
